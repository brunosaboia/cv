{# One line per {label, items} group, labels and order taken from the data.
   group["items"], not group.items -- on a dict, Jinja resolves `.items` to
   the built-in method before it looks for the key. #}
{% for group in skills %}
{{ group.label }}: {{ group["items"] | join(', ') }}
{% endfor %}
